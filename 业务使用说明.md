# 羽毛球直播大屏 · 业务使用说明

本文面向 **运营、导播、业务员** 等非开发人员：您只需要维护 **一个配置文件**，即可在已打开的大屏页面上 **实时更新** 文字和图片（保存文件后约零点几秒生效）。

---

## 一、改哪里？

所有可配置内容都在项目里的这个文件：

**`config/display.json`**

- 用 **记事本、VS Code、Cursor** 等文本编辑器打开即可。
- 改完后 **保存文件**（Ctrl+S）。
- 大屏电脑上的页面需已用 **本站服务** 打开（见技术同事提供的地址，一般是本机 `http://localhost:5173/badminton.html`）。保存后 **不用刷新网页**，角标会短暂显示「已同步」类提示。

> **不要** 改文件名、不要移动文件位置；不确定时先 **复制一份备份** 再编辑。

---

## 二、文件结构（只要懂两层）

`display.json` 里主要有两块：

| 区块 | 作用 |
|------|------|
| **`copy`** | 所有 **文字**（对阵、比分、机位说明、侧栏场次等） |
| **`images`** | **图片路径**（顶栏条、记分牌底图、右侧预告底图、页脚图等） |

每一项都是 **英文键名 + 冒号 + 内容**，文字必须写在 **英文双引号** 里。

---

## 三、常用文字键（`copy`）

### 1. 右上记分牌（当前这场）

| 键名 | 大屏上对应内容 |
|------|----------------|
| `court-scoreboard__discipline` | 项目类别，如 **男双 / 混双 / 男单** |
| `badminton_pair_home` | 主队（左侧）选手姓名 |
| `badminton_score_home` | 主队大分（数字即可，如 `12`） |
| `badminton_score_away` | 客队大分 |
| `badminton_pair_away` | 客队（右侧）选手姓名 |

### 2. 下方机位按钮文案

| 键名 | 说明 |
|------|------|
| `badminton_cam1` | 机位 1 按钮上显示的文字 |
| `badminton_cam2` | 机位 2 |
| `badminton_cam3` | 机位 3 |

### 3. 右侧「三场预告」每一块（共三场）

每场都有类似的一组键（下面的 `1` 可换成 `2`、`3`）：

| 键名 | 说明 |
|------|------|
| `badminton_match1_meta` | 第一场：项目类别 |
| `badminton_match1_match_time` | 第一场：预告时间或状态说明 |
| `badminton_match1_a` / `badminton_match1_b` | 第一场：左右双方姓名 |
| `badminton_match1_score1` ~ `score3` | 第一场：三局比分（如 `21:18`）；**不配第三键或留空** 则页面不显示第三局 |
| `badminton_match1_default` | 当三场比分 **都为空** 时显示的占位句（如「敬请期待」） |

第二场、第三场把键名里的 **`match1`** 改成 **`match2`**、 **`match3`** 即可。

### 4. 页脚等区域

当前配置里还可能包含 **`badminton_foot_rules_h_cn`** 等页脚相关键，按实际需要修改文字即可；若某项不在您的 `display.json` 里，以大屏为准或向技术索要最新模板。

---

## 四、图片（`images`）

一般不推荐业务同事改路径；如需换物料图：

- 文件放在 **`public/assets/`** 下（或由技术告诉您具体目录）。
- 在 `images` 里填写 **以 `/` 开头的路径**，例如：`"/assets/footer_2.png"`。
- 改完同样 **保存**；若无变化，请先 **硬性刷新** 浏览器（Ctrl+F5）再试图片缓存问题。

常用键示例：

- `badminton_img_scoreboard`：记分牌底板图  
- `badminton_img_sidebar`：右侧预告区域底板图  
- `badminton_img_footer`：页脚条图  

---

## 五、编辑时务必注意（避免整页报错）

1. **双引号**：所有文案都要用英文 `"`，不要用中文 「」包裹键名。
2. **逗号**：每一项之间要有 **逗号**；最后一个项目后面 **不要** 多写一个逗号。
3. **不要删错大括号**：`{` `}`、`copy`/`images` 结构不要删乱。
4. 若保存后角标提示 **配置错误**，说明 JSON 格式坏了——请 **撤销修改** 或 **用备份替换**，再找技术看一眼。

---

## 六、和 GitHub（公开仓库）的关系

项目在 GitHub **公开** 可查：<https://github.com/Facefall/screen-live-html>  

对外公开的是 **源代码与示例配置**。  
**临场直播用的 `display.json` 仍放在您这边的电脑/服务器上编辑**；如您希望版本留痕，可请技术帮您把改过后的配置也纳入仓库或另行备份。

---

## 七、需要找技术同事的情况

- 首次安装 Node、启动命令、局域网别的电脑如何打开大屏；
- JSON 报错无法恢复、`display.json` 需要恢复默认模板；
- 更换全新 HTML 版面、增减字段按钮等 **结构级** 需求。

如您只有 **改字、改数字、微调预告时间**，按第三节对照键名修改即可优先自助完成。
